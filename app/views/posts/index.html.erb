<h1>ツイート一覧</h1>
<table>
<% @posts.each do |post| %>
  <tr>
    <th>内容</th>
  </tr>
  <tr>
    <td><%= post.content %></td>
  </tr>
  <tr>
    <td><%= link_to 'ツイートの詳細', post_path(post.id) %></td>
  </tr>
<% end %>
</table>
<%= link_to 'ツイートする',new_post_path %>
